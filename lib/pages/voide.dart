import 'package:flutter/material.dart';

class Voide extends StatelessWidget {
  const Voide({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("商品"),
    );
  }
}
